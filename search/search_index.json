{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"grok-install \u2014 the open standard for installable AI agents","text":"<p>Open standard \u00b7 v2.12</p> ~/my-agent"},{"location":"#one-yaml-file-install-any-ai-agent-on-x","title":"One YAML file. Install any AI agent on X.","text":"<p> grok-install is the open specification for describing, scanning, and shipping Grok-powered agents straight from a GitHub repo. Multi-agent swarms, safety profiles, and every tool schema \u2014 in plain YAML. </p>      Build your first agent \u2192         Read the spec"},{"location":"#why-grok-install","title":"Why grok-install","text":""},{"location":"#one-click-installable","title":"One-click installable","text":"<p>Any repo with a <code>grok-install.yaml</code> becomes installable on X. Paste the link, tap Install with Grok, done.</p>"},{"location":"#multi-agent-swarms","title":"Multi-agent swarms","text":"<p>First-class support for sequential, conditional, and parallel agent orchestration via <code>grok-workflow.yaml</code> \u2014 no glue code.</p>"},{"location":"#safety-profiles","title":"Safety profiles","text":"<p>Declare <code>standard</code> or <code>strict</code>. Approval gates, rate limits, and permission scopes are enforced by the runtime, not by convention.</p>"},{"location":"#multi-llm","title":"Multi-LLM","text":"<p><code>grok-4</code> is the default, but drop in OpenAI, Anthropic, Ollama, or any custom endpoint by changing a single field.</p>"},{"location":"#deploy-anywhere","title":"Deploy anywhere","text":"<p>Vercel, Railway, Docker, Replit \u2014 the CLI generates the config for each. Run locally with <code>grok-install run</code>.</p>"},{"location":"#certified-templates","title":"Certified templates","text":"<p>Ten production-ready templates in awesome-grok-agents \u2014 each safety-scanned, CI-validated, and copy-paste ready.</p>"},{"location":"#quick-start","title":"Quick start","text":"<ol> <li> Install the CLI <pre><code>pip install grok-install\n</code></pre> </li> <li> Scaffold a new agent <pre><code>grok-install init my-agent\ncd my-agent\n</code></pre>  This creates <code>grok-install.yaml</code> + <code>.grok/</code> with <code>grok-agent.yaml</code>, <code>grok-prompts.yaml</code>, and <code>grok-security.yaml</code>.  </li> <li> Run it locally <pre><code>export XAI_API_KEY=sk-...\ngrok-install run .\n</code></pre>  Your agent is now live on <code>localhost</code>. Iterate until you're happy.  </li> <li> Publish on X  Push the repo, post the link, and tag <code>@grok</code>. The runtime scans it, verifies safety, and shows an Install with Grok button to every reader.  </li> </ol> <p>Try the 10-minute tutorial \u2192 Open the playground \u2192</p>"},{"location":"#where-to-go-next","title":"Where to go next","text":""},{"location":"#full-spec-reference","title":"Full spec reference \u2192","text":"<p>Every field, every default, every constraint across all five YAML file types in v2.12.</p>"},{"location":"#cli-reference","title":"CLI reference \u2192","text":"<p><code>init</code>, <code>validate</code>, <code>scan</code>, <code>run</code>, <code>test</code>, <code>deploy</code>, <code>install</code>, <code>publish</code> \u2014 with flags and examples.</p>"},{"location":"#multi-agent-swarms_1","title":"Multi-agent swarms \u2192","text":"<p>Patterns for researcher-critic-publisher, triage-draft-review, and other production flows.</p>"},{"location":"#for-xai","title":"For xAI \u2192","text":"<p>Considering this as an official standard? Read the adoption guide.</p>"},{"location":"contributing/","title":"Contributing","text":"<p>Four places to contribute, matching the four repos in the ecosystem.</p> Area Repo What lives there Spec <code>grok-install</code> Standard text + examples Schemas <code>grok-yaml-standards</code> JSON Schemas, versioned CLI <code>grok-install-cli</code> <code>grok-install</code> command Templates <code>awesome-grok-agents</code> Certified agent templates Docs <code>grok-docs</code> \u2014 this site Everything you're reading"},{"location":"contributing/#spec-changes","title":"Spec changes","text":"<p>Open a PR against <code>grok-yaml-standards</code> that includes:</p> <ol> <li>Schema diff</li> <li>Example YAML using the new field</li> <li>A note under the appropriate version section</li> </ol> <p>Breaking changes bump the major version (<code>v2.x</code> \u2192 <code>v3.0</code>), add a migration note, and ship with a codemod in the CLI.</p>"},{"location":"contributing/#cli-fixes-and-features","title":"CLI fixes and features","text":"<p>Open an issue on <code>grok-install-cli</code> first, tag with <code>proposal</code>. We'll discuss scope before you start coding. Once agreed:</p> <pre><code>git clone https://github.com/AgentMindCloud/grok-install-cli\ncd grok-install-cli\npip install -e '.[dev]'\npytest\nruff check .\nmypy .\n</code></pre>"},{"location":"contributing/#docs-fixes","title":"Docs fixes","text":"<p>This site is plain Markdown in <code>docs/</code>. Click Edit on GitHub on any page to open the source directly. Run locally:</p> <pre><code>pip install -r requirements.txt\nmkdocs serve\n</code></pre> <p>Then visit <code>http://localhost:8000</code>.</p>"},{"location":"contributing/#new-templates","title":"New templates","text":"<p>Submit to <code>awesome-grok-agents</code> using the template submission PR template. Your template will be validated by CI \u2014 it must:</p> <ul> <li>Pass <code>grok-install validate</code></li> <li>Pass <code>grok-install scan</code> with zero hard errors</li> <li>Include a <code>README.md</code> with a one-paragraph description</li> <li>Include a short demo GIF or screenshot</li> </ul>"},{"location":"contributing/#code-of-conduct","title":"Code of conduct","text":"<p>Be kind. Assume good faith. No crypto scams, no spam agents, no scraping private data. If you're unsure whether your template crosses a line, ask in the issue tracker before writing code.</p>"},{"location":"contributing/#becoming-a-maintainer","title":"Becoming a maintainer","text":"<p>We accept maintainer nominations after three accepted, non-trivial PRs to any repo. Maintainers get review rights on their area of focus and a vote in spec decisions.</p>"},{"location":"cli/reference/","title":"CLI reference","text":"<p>The <code>grok-install</code> CLI parses YAML, scans for safety issues, runs agents locally, and generates deploy configs for every major target.</p>"},{"location":"cli/reference/#install","title":"Install","text":"pippip + xAI extrasfrom source <pre><code>pip install grok-install\n</code></pre> <pre><code>pip install 'grok-install[xai]'\n</code></pre> <pre><code>git clone https://github.com/AgentMindCloud/grok-install-cli\ncd grok-install-cli\npip install -e .\n</code></pre> <p>Check it's installed:</p> <pre><code>grok-install --version\n</code></pre>"},{"location":"cli/reference/#global-flags","title":"Global flags","text":"Flag Description <code>--help</code> Show help for any command. <code>--version</code> Print CLI version. <code>--log-level</code> <code>debug</code> / <code>info</code> / <code>warning</code> / <code>error</code>. Default: <code>info</code>. <code>--no-color</code> Disable ANSI color output. <code>--config PATH</code> Override the <code>~/.grok/config.toml</code> path."},{"location":"cli/reference/#commands","title":"Commands","text":""},{"location":"cli/reference/#grok-install-init","title":"<code>grok-install init</code>","text":"<p>Scaffolds a new agent directory with the five-file layout pre-populated.</p> <pre><code>grok-install init &lt;name&gt; [--template &lt;name&gt;] [--mode simple|advanced]\n</code></pre> Flag Description <code>--template</code> Seed from a template in <code>awesome-grok-agents</code>. Default: <code>hello-grok</code>. <code>--mode</code> <code>simple</code> (skip workflow file) or <code>advanced</code> (full layout). <p>Example:</p> <pre><code>grok-install init my-reply-bot --template reply-engagement-bot\n</code></pre>"},{"location":"cli/reference/#grok-install-validate","title":"<code>grok-install validate</code>","text":"<p>Validates every <code>grok-*.yaml</code> file against its schema plus cross-file references (tool permissions, prompt refs, workflow agent ids).</p> <pre><code>grok-install validate [PATH]\n</code></pre> <p>Exit codes: <code>0</code> success, <code>1</code> validation error, <code>2</code> I/O error.</p>"},{"location":"cli/reference/#grok-install-scan","title":"<code>grok-install scan</code>","text":"<p>Pre-install safety assessment. Inspects tool permissions, network grants, bundled secrets, and third-party dependencies. Emits a summary matching what Grok itself runs before showing the Install with Grok button.</p> <pre><code>grok-install scan [PATH] [--format text|json|sarif]\n</code></pre> <p>Run it in CI</p> <p>Add <code>grok-install scan --format sarif</code> to your workflow; GitHub Security will surface findings inline on PRs.</p>"},{"location":"cli/reference/#grok-install-run","title":"<code>grok-install run</code>","text":"<p>Executes the agent locally. Reads env vars, boots the runtime, and drops you into an interactive session (or runs the scheduled job once if <code>schedule:</code> is set).</p> <pre><code>grok-install run [PATH] [--once] [--watch]\n</code></pre> Flag Description <code>--once</code> Run one iteration and exit. Useful for scheduled agents. <code>--watch</code> Restart on file change. Your iteration loop."},{"location":"cli/reference/#grok-install-test","title":"<code>grok-install test</code>","text":"<p>Dry-run with mock tools. No network, no API keys, no side effects \u2014 just verifies control flow and prompt rendering.</p> <pre><code>grok-install test [PATH] [--input path/to/fixture.json]\n</code></pre>"},{"location":"cli/reference/#grok-install-deploy","title":"<code>grok-install deploy</code>","text":"<p>Generates deployment artifacts for the target of your choice. Does not push \u2014 you still commit the generated files yourself.</p> <pre><code>grok-install deploy [PATH] --target vercel|railway|docker|replit\n</code></pre> VercelRailwayDockerReplit <p>Emits <code>vercel.json</code> + a <code>/api/agent.py</code> handler.</p> <p>Emits <code>railway.json</code>, <code>Procfile</code>, and <code>runtime.txt</code>.</p> <p>Emits <code>Dockerfile</code> + <code>.dockerignore</code> targeting <code>python:3.11-slim</code>.</p> <p>Emits <code>.replit</code> + <code>replit.nix</code>.</p>"},{"location":"cli/reference/#grok-install-install","title":"<code>grok-install install</code>","text":"<p>The inverse of <code>publish</code>. Clone any remote agent repo, run the safety scan, and execute it \u2014 the same flow Grok runs when a user taps Install with Grok on X.</p> <pre><code>grok-install install &lt;github-url-or-owner/repo&gt;\n</code></pre> <p>Example:</p> <pre><code>grok-install install AgentMindCloud/hello-grok\n</code></pre>"},{"location":"cli/reference/#grok-install-publish","title":"<code>grok-install publish</code>","text":"<p>Outputs the metadata block required for submission to <code>awesome-grok-agents</code>. Prints to stdout; pipe into a PR body.</p> <pre><code>grok-install publish [PATH] &gt; metadata.json\n</code></pre>"},{"location":"cli/reference/#python-api","title":"Python API","text":"<p>The CLI is a thin wrapper around the <code>grok_install</code> Python package. Useful if you want to embed validation or scanning in another tool.</p> <pre><code>from grok_install import validate, scan, load\n\nspec = load(\"./my-agent\")\nreport = validate(spec)          # raises on hard errors\nfindings = scan(spec)             # returns list[Finding]\n</code></pre> <p>See the source repo for the full surface.</p>"},{"location":"cli/reference/#troubleshooting","title":"Troubleshooting","text":"Symptom Fix <code>Tool 'x' used by agent but not in permissions</code> Add <code>tool:x</code> to <code>grok-security.yaml</code> \u2192 <code>permissions</code>. <code>Prompt 'y' referenced but not defined</code> Add a <code>y:</code> key under <code>prompts:</code> in <code>grok-prompts.yaml</code>. <code>XAI_API_KEY is not set</code> <code>export XAI_API_KEY=sk-...</code> or add it to <code>.env</code>; don't commit. <code>ConnectError: Permission denied for host z</code> Add <code>network:z</code> to <code>grok-security.yaml</code>. <code>Schedule.interval parse error</code> Use a duration literal: <code>30s</code>, <code>5m</code>, <code>2h</code>, <code>1d</code>."},{"location":"ecosystem/","title":"Ecosystem","text":"<p><code>grok-install</code> is a spec, not a framework. It plays well with the broader AI-agent ecosystem.</p>"},{"location":"ecosystem/#xai-sdk","title":"xAI SDK \u2192","text":"<p>The default runtime. <code>grok-install</code> never re-implements the SDK \u2014 it wraps it.</p>"},{"location":"ecosystem/#litellm","title":"LiteLLM \u2192","text":"<p>Use any provider (OpenAI, Anthropic, Ollama) behind a unified interface.</p>"},{"location":"ecosystem/#semantic-kernel","title":"Semantic Kernel \u2192","text":"<p>Bridge <code>grok-install</code> agents into the Microsoft Semantic Kernel planner.</p>"},{"location":"ecosystem/litellm/","title":"LiteLLM","text":"<p>LiteLLM gives <code>grok-install</code> access to ~100+ providers through a unified API. Useful when:</p> <ul> <li>You want to compare model families on the same agent.</li> <li>Your org standardizes on a different provider.</li> <li>You run a local model via Ollama for dev.</li> </ul>"},{"location":"ecosystem/litellm/#installation","title":"Installation","text":"<pre><code>pip install grok-install litellm\n</code></pre>"},{"location":"ecosystem/litellm/#configure-the-agent","title":"Configure the agent","text":"<pre><code># grok-install.yaml\nllm:\n  provider: litellm\n  model: openai/gpt-4o-mini\n</code></pre> <p>The <code>provider: litellm</code> tells the runtime to route completions through the LiteLLM client. The <code>model:</code> field is a LiteLLM model identifier:</p> Provider Example <code>model</code> OpenAI <code>openai/gpt-4o-mini</code> Anthropic <code>anthropic/claude-sonnet-4-6</code> Ollama (local) <code>ollama/llama3.3</code> Azure OpenAI <code>azure/my-deployment-id</code> Groq <code>groq/llama-3.3-70b-versatile</code>"},{"location":"ecosystem/litellm/#env-vars","title":"Env vars","text":"<p>LiteLLM reads provider creds from the env. Declare them in <code>grok-install.yaml</code> so the install flow prompts the user:</p> <pre><code>env:\n  - OPENAI_API_KEY\n</code></pre>"},{"location":"ecosystem/litellm/#mixing-providers-across-agents","title":"Mixing providers across agents","text":"<pre><code>agents:\n  - id: researcher\n    model: openai/gpt-4o        # OpenAI for heavy lifting\n  - id: critic\n    model: anthropic/claude-sonnet-4-6   # different family for diversity\n  - id: publisher\n    model: ollama/llama3.3      # local, cheap, fast for the final step\n</code></pre> <p>Every agent is routed through LiteLLM; each model identifier gets its own provider credentials from the env.</p>"},{"location":"ecosystem/litellm/#cost-and-latency","title":"Cost and latency","text":"<p>LiteLLM adds a thin wrapping layer (~1\u20133ms). Not a concern for normal agent workloads. If you're invoking the same provider at high volume, prefer <code>provider: xai</code> (or the provider-native SDK) to bypass the router.</p>"},{"location":"ecosystem/semantic-kernel/","title":"Semantic Kernel","text":"<p>Semantic Kernel is Microsoft's AI orchestration framework. If your org already runs SK, you can expose <code>grok-install</code> agents as native SK plugins.</p>"},{"location":"ecosystem/semantic-kernel/#when-to-use-this-bridge","title":"When to use this bridge","text":"<ul> <li>Your existing stack is Semantic Kernel + .NET / C# / Python.</li> <li>You want <code>grok-install</code> agents to appear as SK functions inside a   larger planner.</li> <li>You want to keep the agent's spec (YAML) as the source of truth but   invoke it from SK.</li> </ul>"},{"location":"ecosystem/semantic-kernel/#installation","title":"Installation","text":"<pre><code>pip install grok-install grok-install-semantic-kernel semantic-kernel\n</code></pre>"},{"location":"ecosystem/semantic-kernel/#expose-an-agent-as-an-sk-function","title":"Expose an agent as an SK function","text":"<pre><code>from semantic_kernel import Kernel\nfrom grok_install_sk import GrokInstallPlugin\n\nkernel = Kernel()\n# Mount a local grok-install agent directory as an SK plugin.\nkernel.add_plugin(\n    GrokInstallPlugin.from_directory(\"./research-swarm\"),\n    plugin_name=\"research\",\n)\n\nresult = await kernel.invoke(\n    plugin_name=\"research\",\n    function_name=\"run\",\n    question=\"What are the latest grok-4 benchmarks?\",\n)\n</code></pre> <p>Every workflow or agent defined in your <code>grok-install.yaml</code> becomes an invokable SK function. Input types come from <code>workflow.input_schema</code>.</p>"},{"location":"ecosystem/semantic-kernel/#invoke-from-the-sk-planner","title":"Invoke from the SK planner","text":"<pre><code>from semantic_kernel.planners import SequentialPlanner\n\nplanner = SequentialPlanner(kernel)\nplan = await planner.create_plan(\n    \"Write a research brief on &lt;topic&gt;, then draft an X thread about it.\"\n)\nresult = await plan.invoke(kernel)\n</code></pre> <p>The planner sees the <code>research</code> plugin and composes it with whatever other SK functions you've registered.</p>"},{"location":"ecosystem/semantic-kernel/#caveats","title":"Caveats","text":"<ul> <li>Approval gates from <code>grok-security.yaml</code> still apply. If you call a   strict-profile agent from inside SK, the runtime blocks on approval   \u2014 consider routing approvals through the SK kernel's event system.</li> <li>Tools defined in your agent's <code>tools/</code> directory run in the agent's   sandbox, not SK's. Your SK plugins stay separate.</li> </ul>"},{"location":"ecosystem/xai-sdk/","title":"xAI SDK","text":"<p>The <code>xai-sdk</code> is the default execution backend when <code>llm.provider: xai</code> (or no provider is set). <code>grok-install</code> never re-implements the SDK \u2014 it wraps it, so your SDK version and your runtime version can upgrade independently.</p>"},{"location":"ecosystem/xai-sdk/#installation","title":"Installation","text":"<pre><code>pip install 'grok-install[xai]'\n</code></pre> <p>This installs <code>grok-install</code> + the pinned-compatible <code>xai-sdk</code> version.</p>"},{"location":"ecosystem/xai-sdk/#model-selection","title":"Model selection","text":"<pre><code># grok-install.yaml\nmodel: grok-4\n</code></pre> <p>Overridable per agent:</p> <pre><code># .grok/grok-agent.yaml\nagents:\n  - id: researcher\n    model: grok-4                # best quality\n  - id: summarizer\n    model: grok-4-mini           # faster, cheaper\n</code></pre>"},{"location":"ecosystem/xai-sdk/#passing-sdk-options","title":"Passing SDK options","text":"<pre><code>llm:\n  provider: xai\n  model: grok-4\n  options:\n    max_completion_tokens: 4096\n    reasoning_effort: high\n</code></pre> <p>Anything under <code>options:</code> is passed through to the SDK's <code>client.chat.completions.create()</code> call unchanged. Check the xAI SDK docs for the current option list.</p>"},{"location":"ecosystem/xai-sdk/#direct-sdk-access-in-tools","title":"Direct SDK access in tools","text":"<p>Inside a tool, you can import the SDK directly if you need raw access:</p> <pre><code>from grok_install.tools import tool\nfrom xai_sdk import Client\n\n@tool(name=\"summarize\", ...)\ndef summarize(text: str) -&gt; str:\n    client = Client()\n    resp = client.chat.completions.create(\n        model=\"grok-4-mini\",\n        messages=[{\"role\": \"user\", \"content\": f\"Summarize: {text}\"}],\n    )\n    return resp.choices[0].message.content\n</code></pre> <p>The runtime handles auth, retries, and rate limiting; your tool just calls the SDK.</p>"},{"location":"ecosystem/xai-sdk/#compatibility","title":"Compatibility","text":"<code>grok-install</code> <code>xai-sdk</code> Notes <code>&gt;=2.12</code> <code>&gt;=0.4.0</code> Current. <code>&gt;=2.10, &lt;2.12</code> <code>&gt;=0.3.0</code> <code>reasoning_effort</code> not exposed. <code>&gt;=2.0, &lt;2.10</code> <code>&gt;=0.2.0</code> Legacy."},{"location":"for-xai/adoption-guide/","title":"Adoption guide \u2014 for xAI","text":"<p>Official standard candidate. This page is written for the xAI team. It describes what it would look like for <code>grok-install</code> to become an officially recognized standard \u2014 and what we're not asking for.</p>"},{"location":"for-xai/adoption-guide/#adoption-guide-for-xai","title":"Adoption guide \u2014 for xAI","text":""},{"location":"for-xai/adoption-guide/#the-30-second-pitch","title":"The 30-second pitch","text":"<p><code>grok-install</code> is the YAML spec that lets any GitHub repo become a one-click installable AI agent on X. It's been live for four months, has a working CLI, a certified template gallery, and real users shipping real agents. We've built it deliberately as an open standard, not as a company, not as a product.</p> <p>If xAI recognizes it \u2014 even lightly \u2014 it becomes the default path for every agent builder in the Grok ecosystem.</p>"},{"location":"for-xai/adoption-guide/#current-footprint-as-of-april-2026","title":"Current footprint (as of April 2026)","text":"<ul> <li>Standard: <code>grok-install</code>,   v2.12.</li> <li>Schemas:   <code>grok-yaml-standards</code>,   v1.2.0, MIT-licensed.</li> <li>CLI:   <code>grok-install-cli</code>   \u2014 <code>pip install grok-install</code>.</li> <li>Templates:   <code>awesome-grok-agents</code>   \u2014 10 certified, CI-validated templates across single-agent,   multi-step, and swarm patterns.</li> <li>Docs (you're reading them):   <code>grok-docs</code>.</li> <li>License: Apache 2.0 across the ecosystem. Permissively reusable by xAI,   by contributors, by anyone.</li> </ul>"},{"location":"for-xai/adoption-guide/#why-this-exists","title":"Why this exists","text":"<p>Every agent builder on X today is reinventing the same five things:</p> <ol> <li>How to describe an agent so someone else can install it.</li> <li>How to declare what tools it's allowed to call.</li> <li>How to run safety checks before handing it user creds.</li> <li>How to orchestrate two or more agents together.</li> <li>How to deploy it somewhere that doesn't cost $200/month idle.</li> </ol> <p><code>grok-install</code> solves all five in YAML. Five files, one spec, one CLI, one install flow. The goal was always that xAI shouldn't have to build this themselves.</p>"},{"location":"for-xai/adoption-guide/#what-adoption-could-look-like-menu","title":"What adoption could look like (menu)","text":"<p>These are options, in rough order of lightness. You pick what fits.</p>"},{"location":"for-xai/adoption-guide/#1-link-from-xai-docs","title":"1. Link from xAI docs","text":"<p>Add a \"Build an installable agent\" section to https://docs.x.ai that links here. Zero engineering work. Massive signal.</p>"},{"location":"for-xai/adoption-guide/#2-register-the-grok-install-repo-command","title":"2. Register the <code>@grok install &lt;repo&gt;</code> command","text":"<p>When a user posts <code>@grok install &lt;github-url&gt;</code>, have Grok dispatch to the <code>grok-install</code> scan + runtime. The spec already describes the exact fields needed; we've tested the flow end-to-end locally.</p>"},{"location":"for-xai/adoption-guide/#3-host-a-registry-endpoint","title":"3. Host a registry endpoint","text":"<p>A simple <code>https://x.ai/grok-install/&lt;owner&gt;/&lt;repo&gt;</code> redirect that fetches the manifest, renders the install card server-side, and shows it in the X timeline. The scan can remain ours (we already run it); the UI is xAI's.</p>"},{"location":"for-xai/adoption-guide/#4-co-maintain-the-spec","title":"4. Co-maintain the spec","text":"<p>Add an xAI engineer to the spec repo with review rights. We keep running the CLI, docs, and gallery; xAI has veto on spec changes that affect the install flow.</p>"},{"location":"for-xai/adoption-guide/#5-build-it-in-house","title":"5. Build it in-house","text":"<p>The spec and schemas are Apache 2.0. Fork and run, no questions asked. We'd rather co-exist, but we're explicitly not trying to own the thing \u2014 only to make sure it exists.</p>"},{"location":"for-xai/adoption-guide/#what-were-not-asking-for","title":"What we're not asking for","text":"<ul> <li>No compute credits. We run on our own dime.</li> <li>No preferential xAI API access. We use the public API.</li> <li>No payment or revenue share. The standard is free and will   stay free.</li> <li>No exclusivity. LiteLLM, OpenAI, Anthropic, Ollama providers are   all first-class. That's by design.</li> <li>No control over <code>@grok</code>. That's xAI's namespace; this spec   respects it.</li> </ul>"},{"location":"for-xai/adoption-guide/#integration-surface","title":"Integration surface","text":"<p>The places where <code>grok-install</code> touches xAI-owned surfaces:</p> Surface What we'd like What we provide <code>@grok install &lt;repo&gt;</code> Native command recognition Scan API + install runtime (MIT) Install with Grok button on posts Rendered when <code>grok-install.yaml</code> detected Pre-install scan output, manifest parser Grok agent dashboard Lists installed agents <code>grok-install publish</code> metadata format <code>x.ai/gallery</code> Show curated templates <code>awesome-grok-agents</code> as the seed gallery xAI SDK Keep it as-is; we wrap, not replace Compatibility matrix in ecosystem/xai-sdk <p>Nothing here requires new xAI infra. Most of it is \"turn on the feature flag that recognizes the file, redirect to our scan endpoint.\"</p>"},{"location":"for-xai/adoption-guide/#faq","title":"FAQ","text":"Why isn't this a company? <p>Because it shouldn't be. Standards get adopted when they're neutral. If <code>grok-install</code> were a VC-funded company, xAI would (correctly) build its own. By staying an Apache 2.0 standard we remove the reason to compete with it.</p> How do you pay for it? <p>We don't \u2014 it's under 1 engineer's weekends. The CLI is free, docs are free, gallery is free. If xAI ever wanted to sponsor maintenance, that'd be welcome; if not, it runs fine as-is.</p> What if the spec needs to evolve? <p>It has, four times. v1 \u2192 v2.12 over four months, with a clean migration at every bump. Spec changes happen in the open on <code>grok-yaml-standards</code>.</p> What's the governance model? <p>Currently a BDFL (myself). If adoption happens, we'd move to a three-person maintainer group, explicitly welcoming an xAI-nominated maintainer if xAI wants one.</p> What's in it for you? <p>Honestly: I want more installable agents on X. That's it. If the standard becomes xAI-native, every developer building on Grok benefits, and the agent ecosystem gets a boost \u2014 which is the goal.</p>"},{"location":"for-xai/adoption-guide/#contact","title":"Contact","text":"<ul> <li>On X: @JanSol0s</li> <li>GitHub: AgentMindCloud</li> <li>Email: jan@agentmindcloud.dev</li> </ul> <p>I'm happy to hop on a 20-minute call with anyone at xAI to walk through the spec, the CLI, and what \"adoption\" could mean for you. No deck, no pitch \u2014 just the facts and a working demo.</p>"},{"location":"gallery/","title":"Templates","text":"<p>Every template in <code>awesome-grok-agents</code> is CI-validated, safety-scanned, and ships with a working demo.</p> <p>Grouped by orchestration pattern:</p>"},{"location":"gallery/#single-agent","title":"Single-agent \u2192","text":"<p>One agent, one job. Start here.</p>"},{"location":"gallery/#multi-step","title":"Multi-step \u2192","text":"<p>Sequential pipelines \u2014 triage \u2192 draft \u2192 publish.</p>"},{"location":"gallery/#swarm","title":"Swarm \u2192","text":"<p>Collaborative patterns \u2014 researcher + critic + publisher.</p>"},{"location":"gallery/#install-any-template-locally","title":"Install any template locally","text":"<pre><code>grok-install install AgentMindCloud/&lt;template-name&gt;\n</code></pre> <p>Or clone from the gallery repo:</p> <pre><code>git clone https://github.com/AgentMindCloud/awesome-grok-agents\ncd awesome-grok-agents/templates/&lt;template-name&gt;\ngrok-install run .\n</code></pre>"},{"location":"gallery/#contribute-a-template","title":"Contribute a template","text":"<p>See the submission guide in the gallery repo. Your template gets CI-validated and safety-scanned on every PR.</p>"},{"location":"gallery/multi-step/","title":"Multi-step templates","text":"<p>Agents chained into a linear pipeline by <code>grok-workflow.yaml</code>. Each step has a narrow job; the workflow wires them together.</p>"},{"location":"gallery/multi-step/#reply-engagement-bot","title":"reply-engagement-bot","text":"<p>Drafts replies to X mentions with an approval gate before posting.</p> <p>Pattern: multi-step \u00b7 Profile: strict</p> <p>Triage classifies the mention, drafter writes a response, publisher posts \u2014 gated by human approval.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/multi-step/#trend-to-thread","title":"trend-to-thread","text":"<p>Monitors X trends and generates full threads on demand.</p> <p>Pattern: multi-step \u00b7 Profile: strict</p> <p>Detector \u2192 researcher \u2192 thread-writer \u2192 reviewer \u2192 publisher.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/multi-step/#code-reviewer","title":"code-reviewer","text":"<p>Adds inline comments to GitHub pull requests.</p> <p>Pattern: multi-step \u00b7 Profile: strict</p> <p>Fetcher pulls the diff, analyzer produces review notes, commenter posts them inline on the PR.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/multi-step/#thread-ghostwriter","title":"thread-ghostwriter","text":"<p>Transforms rough ideas into polished X threads.</p> <p>Pattern: multi-step \u00b7 Profile: strict</p> <p>Outliner \u2192 drafter \u2192 editor \u2192 reviewer.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/multi-step/#personal-knowledge","title":"personal-knowledge","text":"<p>Searchable memory index of your X posting history.</p> <p>Pattern: multi-step \u00b7 Profile: standard</p> <p>Indexer (scheduled) \u2192 retriever \u2192 summarizer.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/multi-step/#voice-agent-x","title":"voice-agent-x","text":"<p>Voice-to-post workflow with a review step.</p> <p>Pattern: multi-step \u00b7 Profile: strict</p> <p>Transcriber \u2192 drafter \u2192 reviewer (approval-gated) \u2192 publisher.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/multi-step/#live-event-commentator","title":"live-event-commentator","text":"<p>Real-time event updates on X.</p> <p>Pattern: multi-step \u00b7 Profile: strict</p> <p>Feed-watcher \u2192 summarizer \u2192 scheduler \u2192 publisher.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/multi-step/#when-to-pick-multi-step","title":"When to pick multi-step","text":"<ul> <li>You want each agent to do one thing well.</li> <li>Your workflow is deterministic top to bottom (with optional <code>when:</code>   gates).</li> <li>You need a human approval step somewhere in the middle.</li> <li>Cost matters \u2014 smaller prompts on each step add up to a smaller bill.</li> </ul>"},{"location":"gallery/single-agent/","title":"Single-agent templates","text":"<p>One <code>grok-agent.yaml</code> with a single entry. No workflow file needed. Best starting point for a new idea.</p>"},{"location":"gallery/single-agent/#hello-grok","title":"hello-grok","text":"<p>The simplest possible Grok agent. Single agent, single tool.</p> <p>Pattern: single-agent \u00b7 Profile: standard</p> <pre><code>spec: grok-install/v2.12\nname: hello-grok\ndescription: The simplest possible Grok agent. Single agent, single tool.\nentrypoint: .grok/grok-agent.yaml\nmodel: grok-4\nruntime:\n  python: \"&gt;=3.11\"\nenv:\n  - XAI_API_KEY\n</code></pre> <p>A greeter with access to a <code>now</code> tool. 40-line footprint end to end. Recommended first template to read.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/single-agent/#when-to-pick-single-agent","title":"When to pick single-agent","text":"<ul> <li>Prototyping a new capability.</li> <li>The behavior fits in one coherent system prompt.</li> <li>You don't need conditional routing between steps.</li> <li>You want zero orchestration overhead.</li> </ul> <p>When you outgrow it, split into multi-step \u2014 no rewrite needed, just add a <code>grok-workflow.yaml</code>.</p>"},{"location":"gallery/swarm/","title":"Swarm templates","text":"<p>Multiple agents collaborating, often with conditional loops. The workflow uses <code>when:</code> clauses to route between agents based on prior output.</p>"},{"location":"gallery/swarm/#research-swarm","title":"research-swarm","text":"<p>Researcher + critic + publisher, with a conditional second research pass.</p> <p>Pattern: swarm \u00b7 Profile: standard</p> <p>The researcher gathers sources, the critic flags weak claims, the researcher refines only if needed, the publisher compiles the final brief. The canonical reference for the critic-loop pattern.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/swarm/#scientific-discovery","title":"scientific-discovery","text":"<p>Daily arXiv + X discussion summaries with a research swarm.</p> <p>Pattern: swarm \u00b7 Profile: standard</p> <p>Scheduler fires daily, fans out to arXiv + X specialists, synthesizer composes the digest post.</p> <p>View on GitHub \u2192</p>"},{"location":"gallery/swarm/#when-to-pick-a-swarm","title":"When to pick a swarm","text":"<ul> <li>Quality matters more than cost.</li> <li>You need iterative refinement (critic loops).</li> <li>Different specialists would produce better results than one   generalist agent (breadth + depth).</li> <li>You're okay with the additional orchestration in <code>grok-workflow.yaml</code>.</li> </ul> <p>Read the pattern deep-dive: Multi-agent swarms \u2192.</p>"},{"location":"getting-started/","title":"Getting started","text":"<p>Three pages. Start to finish in under half an hour.</p>"},{"location":"getting-started/#1-install-the-cli","title":"1. Install the CLI \u2192","text":"<p><code>pip install grok-install</code> plus the two env vars you'll need.</p>"},{"location":"getting-started/#2-your-first-agent","title":"2. Your first agent \u2192","text":"<p>10-minute copy-paste tutorial. Hello-Grok, running locally, validated.</p>"},{"location":"getting-started/#3-deploy-to-production","title":"3. Deploy to production \u2192","text":"<p>Vercel, Railway, Docker, Replit. Pick one. Ship.</p>"},{"location":"getting-started/deploy/","title":"Deploy to production","text":"<p><code>grok-install deploy</code> generates a deployment config for the target you pick. It does not push \u2014 you commit the generated files yourself and trust your platform of choice.</p>"},{"location":"getting-started/deploy/#pick-a-target","title":"Pick a target","text":"Target Best for Cold start Schedules Vercel HTTP agents, webhook handlers ~1.5s Cron Railway Long-running schedulers, scrapers persistent Built-in Docker Self-hosted, on-prem, custom K8s depends External Replit Hackathons, low-stakes shared envs ~3s Replit Cron"},{"location":"getting-started/deploy/#vercel","title":"Vercel","text":"<pre><code>grok-install deploy . --target vercel\n</code></pre> <p>Emits:</p> <ul> <li><code>vercel.json</code> \u2014 route config</li> <li><code>api/agent.py</code> \u2014 Vercel Python runtime handler</li> <li><code>requirements.txt</code> \u2014 pinned deps</li> </ul> <p>Push and deploy:</p> <pre><code>git add vercel.json api/ requirements.txt\ngit commit -m \"deploy: vercel\"\nvercel --prod\n</code></pre> <p>Set <code>XAI_API_KEY</code> in the Vercel dashboard under Settings \u2192 Environment Variables. Any other env var from <code>grok-install.yaml</code> \u2192 <code>env:</code> needs to be added here too.</p>"},{"location":"getting-started/deploy/#railway","title":"Railway","text":"<pre><code>grok-install deploy . --target railway\n</code></pre> <p>Emits <code>railway.json</code>, <code>Procfile</code>, <code>runtime.txt</code>.</p> <pre><code>railway login\nrailway link\nrailway up\n</code></pre> <p>For scheduled agents (<code>schedule.interval: 5m</code>) Railway's built-in cron picks up the <code>worker:</code> process in the Procfile automatically.</p>"},{"location":"getting-started/deploy/#docker","title":"Docker","text":"<pre><code>grok-install deploy . --target docker\ndocker build -t my-agent .\ndocker run --rm -e XAI_API_KEY=$XAI_API_KEY my-agent\n</code></pre> <p>The generated <code>Dockerfile</code> targets <code>python:3.11-slim</code> with a non-root user and a read-only filesystem by default. If your agent writes to disk, flip <code>--read-only</code> off in your run command.</p>"},{"location":"getting-started/deploy/#replit","title":"Replit","text":"<pre><code>grok-install deploy . --target replit\n</code></pre> <p>Commit, then click Import from GitHub in Replit. The <code>.replit</code> file pins the run command.</p>"},{"location":"getting-started/deploy/#common-deployment-pitfalls","title":"Common deployment pitfalls","text":"<p>Missing env var in the platform</p> <p>Every name in <code>grok-install.yaml</code> \u2192 <code>env:</code> must also exist in your platform's secret manager, or the agent refuses to boot.</p> <p>Schedule not firing</p> <p>Railway + Vercel require the schedule to be configured in their system too. <code>schedule:</code> in YAML is the declaration; the platform still needs to actually tick the clock.</p> <p>Network permission denied in production</p> <p><code>grok-security.yaml</code> is enforced identically in dev and prod. If a tool talks to a new host, add it to <code>permissions:</code> and redeploy.</p>"},{"location":"getting-started/first-agent/","title":"Your first agent","text":"<p>Estimated time: 10 minutes. You'll have a working, validated, locally-running agent when you're done.</p>"},{"location":"getting-started/first-agent/#what-youll-build","title":"What you'll build","text":"<p>A greeter agent that says hello and tells the user the current time. It's the simplest possible shape of a <code>grok-install</code> agent: one <code>grok-install.yaml</code> at the root, one <code>.grok/grok-agent.yaml</code> underneath, one prompt, one tool, one safety profile.</p>"},{"location":"getting-started/first-agent/#step-1-scaffold-30-sec","title":"Step 1 \u2014 Scaffold (30 sec)","text":"<pre><code>grok-install init hello-grok --template hello-grok\ncd hello-grok\n</code></pre> <p>Look at what got created:</p> <pre><code>hello-grok/\n\u251c\u2500\u2500 grok-install.yaml\n\u2514\u2500\u2500 .grok/\n    \u251c\u2500\u2500 grok-agent.yaml\n    \u251c\u2500\u2500 grok-prompts.yaml\n    \u2514\u2500\u2500 grok-security.yaml\n</code></pre>"},{"location":"getting-started/first-agent/#step-2-inspect-grok-installyaml-1-min","title":"Step 2 \u2014 Inspect <code>grok-install.yaml</code> (1 min)","text":"<pre><code>spec: grok-install/v2.12\nname: hello-grok\ndescription: The simplest possible Grok agent. Single agent, single tool.\nentrypoint: .grok/grok-agent.yaml\nmodel: grok-4\nruntime:\n  python: \"&gt;=3.11\"\nenv:\n  - XAI_API_KEY\n</code></pre> <p>Everything here is documented in the spec. Notice: no hard-coded keys, no secrets \u2014 just the name of the env var the agent will read.</p>"},{"location":"getting-started/first-agent/#step-3-inspect-grokgrok-agentyaml-1-min","title":"Step 3 \u2014 Inspect <code>.grok/grok-agent.yaml</code> (1 min)","text":"<pre><code>agents:\n  - id: greeter\n    model: grok-4\n    prompt_ref: greeter_system\n    tools:\n      - now\n    max_turns: 4\n</code></pre> <p>One agent called <code>greeter</code>, using the <code>grok-4</code> model, reading its system prompt from <code>grok-prompts.yaml</code>, allowed to call the <code>now</code> tool, capped at 4 turns. Full spec \u2192</p>"},{"location":"getting-started/first-agent/#step-4-inspect-the-prompt-30-sec","title":"Step 4 \u2014 Inspect the prompt (30 sec)","text":"<p><code>.grok/grok-prompts.yaml</code>:</p> <pre><code>prompts:\n  greeter_system: |\n    You are a friendly Grok agent whose only job is to greet the user and,\n    if asked, tell them the current time via the `now` tool. Keep replies\n    to two sentences.\n</code></pre>"},{"location":"getting-started/first-agent/#step-5-inspect-security-30-sec","title":"Step 5 \u2014 Inspect security (30 sec)","text":"<p><code>.grok/grok-security.yaml</code>:</p> <pre><code>safety_profile: standard\npermissions:\n  - tool:now\nrequires_approval: []\nrate_limits:\n  tool_calls_per_minute: 30\n</code></pre> <p>The agent can call <code>now</code> and nothing else. No network. No filesystem. Safety profiles deep dive \u2192</p>"},{"location":"getting-started/first-agent/#step-6-validate-5-sec","title":"Step 6 \u2014 Validate (5 sec)","text":"<pre><code>grok-install validate .\n</code></pre> <p>Expected:</p> <pre><code>\u2713 grok-install.yaml        schema OK\n\u2713 .grok/grok-agent.yaml    schema OK, 1 agent\n\u2713 .grok/grok-prompts.yaml  schema OK, 1 prompt\n\u2713 .grok/grok-security.yaml schema OK, 1 tool\n\u2713 cross-refs: prompt_ref[greeter_system] \u2713, tool[now] \u2713\n</code></pre>"},{"location":"getting-started/first-agent/#step-7-scan-5-sec","title":"Step 7 \u2014 Scan (5 sec)","text":"<pre><code>grok-install scan .\n</code></pre> <p>This is the same pre-install scan Grok runs when a user taps Install with Grok. You'll see: profile, permission summary, any network reach, and verdict.</p>"},{"location":"getting-started/first-agent/#step-8-run-it-2-min","title":"Step 8 \u2014 Run it (2 min)","text":"<pre><code>export XAI_API_KEY=sk-...\ngrok-install run .\n</code></pre> <p>You'll drop into an interactive session:</p> <pre><code>greeter \u203a hello\nHi there \u2014 welcome aboard. It's 14:32 UTC.\ngreeter \u203a _\n</code></pre>"},{"location":"getting-started/first-agent/#step-9-iterate-2-min","title":"Step 9 \u2014 Iterate (2 min)","text":"<p>Change the prompt to taste. Edit <code>.grok/grok-prompts.yaml</code>, make the greeter sarcastic:</p> <pre><code>prompts:\n  greeter_system: |\n    You are a dry, slightly snarky greeter. Still helpful. Two sentences\n    max. If asked the time, call `now`.\n</code></pre> <p>Re-run with <code>--watch</code> to hot-reload on save:</p> <pre><code>grok-install run . --watch\n</code></pre>"},{"location":"getting-started/first-agent/#step-10-publish-1-min","title":"Step 10 \u2014 Publish (1 min)","text":"<p>Commit and push:</p> <pre><code>git init\ngit add .\ngit commit -m \"My first Grok agent\"\ngh repo create hello-grok --public --source=. --push\n</code></pre> <p>Post the link on X with a note. Anyone can tap Install with Grok.</p>"},{"location":"getting-started/first-agent/#where-next","title":"Where next","text":""},{"location":"getting-started/first-agent/#deploy-it","title":"Deploy it \u2192","text":"<p>Vercel, Railway, Docker, Replit \u2014 pick your target.</p>"},{"location":"getting-started/first-agent/#add-a-second-agent","title":"Add a second agent \u2192","text":"<p>Chain two agents together with <code>grok-workflow.yaml</code>.</p>"},{"location":"getting-started/first-agent/#give-it-tools","title":"Give it tools \u2192","text":"<p>Web search, page fetch, custom functions.</p>"},{"location":"getting-started/installation/","title":"Install the CLI","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"Requirement Version Python 3.11+ pip 24.0+ (<code>python -m pip install -U pip</code>) xAI API key Free tier works for local dev"},{"location":"getting-started/installation/#install","title":"Install","text":"pip (recommended)pipx (isolated)with xAI SDK extrasfrom source <pre><code>pip install grok-install\n</code></pre> <pre><code>pipx install grok-install\n</code></pre> <pre><code>pip install 'grok-install[xai]'\n</code></pre> <pre><code>git clone https://github.com/AgentMindCloud/grok-install-cli\ncd grok-install-cli\npip install -e .\n</code></pre>"},{"location":"getting-started/installation/#verify","title":"Verify","text":"<pre><code>grok-install --version\n</code></pre> <p>Expected output:</p> <pre><code>grok-install 2.12.x\n</code></pre>"},{"location":"getting-started/installation/#set-your-api-key","title":"Set your API key","text":"<p>Get a key from https://x.ai. Then:</p> bash / zshfishWindows (PowerShell) <pre><code>export XAI_API_KEY=sk-...\n# Persist it\necho 'export XAI_API_KEY=sk-...' &gt;&gt; ~/.zshrc\n</code></pre> <pre><code>set -Ux XAI_API_KEY sk-...\n</code></pre> <pre><code>[Environment]::SetEnvironmentVariable(\"XAI_API_KEY\", \"sk-...\", \"User\")\n</code></pre> <p>Never commit keys</p> <p><code>grok-install</code> reads keys from the environment \u2014 there's no config file. If you put a key in YAML, the pre-install scanner will fail the build.</p>"},{"location":"getting-started/installation/#shell-completion-optional","title":"Shell completion (optional)","text":"bashzsh <pre><code>grok-install --install-completion bash\n</code></pre> <pre><code>grok-install --install-completion zsh\n</code></pre>"},{"location":"getting-started/installation/#next","title":"Next","text":"<p>Build your first agent \u2192</p>"},{"location":"guides/","title":"Guides","text":"<p>Topical deep-dives. Read whichever maps to what you're building.</p>"},{"location":"guides/#multi-agent-swarms","title":"Multi-agent swarms \u2192","text":"<p>Sequential, conditional, and parallel patterns with <code>grok-workflow.yaml</code>.</p>"},{"location":"guides/#tool-schemas","title":"Tool schemas \u2192","text":"<p>How Grok tools are declared, validated, and invoked.</p>"},{"location":"guides/#safety-profiles","title":"Safety profiles \u2192","text":"<p><code>standard</code> vs <code>strict</code>, approval gates, and rate-limit strategy.</p>"},{"location":"guides/#deployment","title":"Deployment \u2192","text":"<p>Picking a target, secret hygiene, logging.</p>"},{"location":"guides/#x-integration","title":"X integration \u2192","text":"<p>Install-on-X flow, <code>@grok</code> commands, marketplace visibility.</p>"},{"location":"guides/deployment/","title":"Deployment","text":"<p>This guide goes beyond \"run <code>deploy --target</code>\". It covers the operational pieces you need for real traffic.</p> <p>See Getting started \u2192 Deploy first for the per-target quickstart.</p>"},{"location":"guides/deployment/#secret-management","title":"Secret management","text":"<p>Rule: no secrets in YAML. The pre-install scanner fails the build if it sees anything that looks like a key.</p> <ul> <li>Dev: <code>export XAI_API_KEY=...</code> in your shell, or <code>.env</code> (gitignored).</li> <li>Vercel / Netlify: Platform env var dashboard.</li> <li>Railway / Render: Service-level env vars.</li> <li>Docker: <code>--env-file</code> at run time, or inject via orchestrator   (Kubernetes secrets, Nomad, ECS task definitions).</li> <li>CI (GitHub Actions): <code>secrets.XAI_API_KEY</code> \u2192 jobs that call   <code>grok-install scan</code> or <code>grok-install test</code>.</li> </ul> <p>Every key in <code>grok-install.yaml</code> \u2192 <code>env:</code> must resolve at agent boot or the runtime refuses to start.</p>"},{"location":"guides/deployment/#logs","title":"Logs","text":"<p>The runtime emits structured JSON to stdout. Minimal shape:</p> <pre><code>{\"ts\":\"2026-04-17T14:32:00Z\",\"level\":\"info\",\"event\":\"agent.tool.call\",\"agent\":\"greeter\",\"tool\":\"now\",\"dur_ms\":3}\n</code></pre> <p>Pipe to whatever your platform aggregates (Vercel Logs, Railway Logs, Datadog, Loki, Cloud Logging). All runtime events are <code>grok.*</code>-prefixed, so a single filter separates agent traffic from framework noise.</p>"},{"location":"guides/deployment/#scheduled-agents","title":"Scheduled agents","text":"<p>If <code>grok-install.yaml</code> declares:</p> <pre><code>schedule:\n  interval: 5m\n</code></pre> <p>...the agent boots, runs one iteration, and exits. Your platform's scheduler ticks it. Generated deploy configs wire this for you on Railway and Vercel Cron; Docker deployments need an external scheduler (cron, systemd timer, Kubernetes CronJob).</p>"},{"location":"guides/deployment/#cold-start","title":"Cold start","text":"Platform Typical cold start Notes Vercel ~1.5s Use <code>grok-4-mini</code> for sub-second first-token. Railway None (warm) Always-on dyno; pay for idle time. Docker Depends You own the scaling story. Replit ~3s Good for personal, not for production traffic."},{"location":"guides/deployment/#rollback","title":"Rollback","text":"<p>Agents are pure YAML + a Python tool directory. To roll back, revert the commit and redeploy. The runtime doesn't hold onto state across versions unless you explicitly write to a persistent store.</p> <p>If you need blue-green behavior: deploy the new version to a second endpoint, smoke-test with <code>grok-install install &lt;repo&gt;@&lt;sha&gt;</code> locally, then swap the user-facing route.</p>"},{"location":"guides/deployment/#observability","title":"Observability","text":"<p>Add your own spans inside tools:</p> <pre><code>from grok_install.obs import span\n\n@tool(name=\"web_search\", ...)\ndef web_search(query: str):\n    with span(\"web_search.tavily\", query_len=len(query)):\n        return _tavily(query)\n</code></pre> <p>Spans emit to stdout as JSON by default. Export to OTel with:</p> <pre><code>export GROK_OBS_EXPORTER=otel\nexport OTEL_EXPORTER_OTLP_ENDPOINT=https://...\n</code></pre>"},{"location":"guides/deployment/#cost-guardrails","title":"Cost guardrails","text":"<p>Three levers, all in <code>grok-security.yaml</code>:</p> <pre><code>rate_limits:\n  tool_calls_per_minute: 30\n  web_fetches_per_run: 60\n  total_tokens_per_run: 100000\n</code></pre> <p>And one in <code>grok-agent.yaml</code> \u2014 <code>max_turns: N</code> caps runaway reason-then-call loops at the conversation level.</p>"},{"location":"guides/deployment/#zero-downtime-config-changes","title":"Zero-downtime config changes","text":"<p>Most edits are safe to hot-deploy. The exceptions:</p> Change Safe? Why Prompt text \u2713 Reloaded on next invocation. Tool added + permission added \u2713 New capability, no existing behavior broken. Tool removed from <code>permissions:</code> \u2717 In-flight runs referencing the tool will 500. <code>spec:</code> bumped across minor version \u2717 Re-validate in staging first. <code>safety_profile</code> strict \u2192 standard \u2717 Re-review the scan output before shipping."},{"location":"guides/multi-agent-swarms/","title":"Multi-agent swarms","text":"<p>When one agent isn't enough. This guide covers the three production patterns we see most: pipeline, critic loop, and fan-out.</p>"},{"location":"guides/multi-agent-swarms/#the-building-blocks","title":"The building blocks","text":"<ul> <li><code>grok-agent.yaml</code> declares the roles.</li> <li><code>grok-workflow.yaml</code> wires them up.</li> <li><code>entrypoint: .grok/grok-workflow.yaml</code> in your root manifest   activates the workflow runtime.</li> </ul>"},{"location":"guides/multi-agent-swarms/#pattern-1-pipeline","title":"Pattern 1 \u2014 Pipeline","text":"<p>Three agents in series. Each step's output feeds the next.</p> <pre><code>flowchart LR\n  A[triage] --&gt; B[drafter] --&gt; C[publisher]</code></pre> <pre><code># .grok/grok-agent.yaml\nagents:\n  - id: triage\n    model: grok-4\n    prompt_ref: triage_system\n    tools: [fetch_mentions, classify_mention]\n    max_turns: 6\n  - id: drafter\n    model: grok-4\n    prompt_ref: drafter_system\n    tools: [draft_reply]\n    max_turns: 4\n  - id: publisher\n    model: grok-4\n    prompt_ref: publisher_system\n    tools: [post_reply]\n    max_turns: 2\n</code></pre> <pre><code># .grok/grok-workflow.yaml\nworkflow:\n  id: reply_pipeline\n  input_schema:\n    mention_id: string\n  steps:\n    - id: classify\n      agent: triage\n      input: { mention_id: \"{{ input.mention_id }}\" }\n      output: classification\n    - id: draft\n      agent: drafter\n      input: { classification: \"{{ classification }}\" }\n      output: draft_text\n    - id: publish\n      agent: publisher\n      input: { text: \"{{ draft_text }}\" }\n      output: posted\n</code></pre> <p>Best for: linear workflows with distinct responsibilities (content moderation, reply bots, doc transformation).</p>"},{"location":"guides/multi-agent-swarms/#pattern-2-critic-loop","title":"Pattern 2 \u2014 Critic loop","text":"<p>A researcher produces, a critic reviews, the researcher refines if needed, a publisher finalizes.</p> <pre><code>flowchart LR\n  A[researcher] --&gt; B[critic]\n  B -- weak_claims --&gt; A\n  B --&gt; C[publisher]</code></pre> <pre><code># .grok/grok-workflow.yaml\nworkflow:\n  id: research_swarm\n  input_schema:\n    question: string\n  steps:\n    - id: research_round_one\n      agent: researcher\n      input: { question: \"{{ input.question }}\" }\n      output: initial_findings\n\n    - id: critique\n      agent: critic\n      input: { findings: \"{{ initial_findings }}\" }\n      output: critiques\n\n    - id: research_round_two\n      agent: researcher\n      when: \"{{ critiques.weak_claims | length &gt; 0 }}\"\n      input:\n        question: \"{{ input.question }}\"\n        gaps: \"{{ critiques.weak_claims }}\"\n      output: refined_findings\n\n    - id: final_brief\n      agent: publisher\n      input:\n        findings: \"{{ refined_findings | default(initial_findings) }}\"\n        critiques: \"{{ critiques }}\"\n      output: brief\n</code></pre> <p>Notice the <code>when:</code> \u2014 it's a Jinja boolean. If the critic found no weak claims, the researcher's second pass is skipped, and the publisher uses the original findings via <code>default()</code>.</p> <p>Best for: quality-sensitive outputs (research briefs, technical threads, anything where fabrication is a risk).</p>"},{"location":"guides/multi-agent-swarms/#pattern-3-fan-out","title":"Pattern 3 \u2014 Fan-out","text":"<p>One dispatcher, N specialists, one synthesizer.</p> <pre><code>flowchart LR\n  A[dispatcher] --&gt; B[specialist_a]\n  A --&gt; C[specialist_b]\n  A --&gt; D[specialist_c]\n  B --&gt; E[synthesizer]\n  C --&gt; E\n  D --&gt; E</code></pre> <p><code>grok-workflow.yaml</code> currently executes steps sequentially; true parallelism is on the v2.13 roadmap. For now, simulate fan-out with independent input mappings:</p> <pre><code>steps:\n  - id: route\n    agent: dispatcher\n    input: { query: \"{{ input.query }}\" }\n    output: routing\n\n  - id: fetch_news\n    agent: news_specialist\n    when: \"{{ 'news' in routing.topics }}\"\n    input: { query: \"{{ input.query }}\" }\n    output: news_results\n\n  - id: fetch_papers\n    agent: paper_specialist\n    when: \"{{ 'research' in routing.topics }}\"\n    input: { query: \"{{ input.query }}\" }\n    output: paper_results\n\n  - id: synthesize\n    agent: synthesizer\n    input:\n      news: \"{{ news_results | default({}) }}\"\n      papers: \"{{ paper_results | default({}) }}\"\n    output: final\n</code></pre>"},{"location":"guides/multi-agent-swarms/#tips","title":"Tips","text":"<p>Keep prompts narrow</p> <p>A 3-agent swarm with focused prompts beats a 1-agent mega-prompt every time. The model doesn't hold 500 lines of instruction well.</p> <p>Bind every step's output</p> <p>Always set <code>output:</code> \u2014 even if you don't think you'll use it. You'll want it for debugging, and <code>--watch</code> runs show every binding.</p> <p>Conditional &gt; re-prompting</p> <p>Prefer a <code>when:</code> gate on a second step over a prompt that says \"try again if X\". Determinism is free.</p>"},{"location":"guides/multi-agent-swarms/#further-reading","title":"Further reading","text":"<ul> <li><code>grok-workflow.yaml</code> reference \u2192</li> <li><code>research-swarm</code> template</li> <li><code>reply-engagement-bot</code> template</li> </ul>"},{"location":"guides/safety-profiles/","title":"Safety profiles","text":"<p>The runtime treats <code>grok-security.yaml</code> as the single source of truth for what an agent is allowed to do. Profiles are shorthand for sensible defaults; the explicit keys always win.</p>"},{"location":"guides/safety-profiles/#standard","title":"<code>standard</code>","text":"<p>The research-friendly default. Tools are allowed if listed in <code>permissions:</code>. Networking is allowed if the host is listed. Rate limits are enforced, but no tool requires approval unless you add it.</p> <p>Good for: research, drafting, internal tools, anything read-heavy.</p> <pre><code>safety_profile: standard\npermissions:\n  - tool:web_search\n  - tool:fetch_page\n  - network:*\nrate_limits:\n  tool_calls_per_minute: 40\n  web_fetches_per_run: 60\n</code></pre>"},{"location":"guides/safety-profiles/#strict","title":"<code>strict</code>","text":"<p>Every destructive or outbound action is gate-walled. The runtime is more conservative with rate limits, and the CLI scanner treats <code>network:*</code> as a warning instead of a quiet pass.</p> <p>Good for: anything that posts, pays, deletes, or messages users.</p> <pre><code>safety_profile: strict\npermissions:\n  - tool:fetch_mentions\n  - tool:draft_reply\n  - tool:post_reply\n  - network:api.twitter.com\nrequires_approval:\n  - post_reply\nrate_limits:\n  tool_calls_per_minute: 15\n</code></pre>"},{"location":"guides/safety-profiles/#approval-gates","title":"Approval gates","text":"<p>A tool in <code>requires_approval:</code> pauses the run. The operator sees:</p> <pre><code>\u23f8 post_reply about to run\n   args:\n     text: \"Thanks for the heads-up \u2014 I'll look into it.\"\n     mention_id: \"1810000000000000001\"\n   [a]pprove  [s]kip  [q]uit\n</code></pre> <p><code>a</code> runs the tool. <code>s</code> returns a <code>tool_skipped</code> signal to the model so it can adapt. <code>q</code> aborts the run.</p> <p>In non-interactive deploys (Vercel, Railway), approval gates route to a queue \u2014 configure a webhook with <code>GROK_APPROVAL_WEBHOOK_URL</code> and wire it to Slack, Discord, or email.</p>"},{"location":"guides/safety-profiles/#rate-limits","title":"Rate limits","text":"Key What it caps When it trips <code>tool_calls_per_minute</code> Every tool invocation across the agent Hot loops, runaway reasoning <code>web_fetches_per_run</code> Network egress Scraper abuse, API bill surprises <code>total_tokens_per_run</code> LLM tokens consumed in one run Runaway context growth <p>Tripping a limit raises a tool-use error back to the model (\"rate limit reached \u2014 try again in 37s\"), not a hard crash. The agent can choose to stop or keep going with different tools.</p>"},{"location":"guides/safety-profiles/#scoping-permissions","title":"Scoping permissions","text":"<p>Format: <code>&lt;scope&gt;:&lt;target&gt;</code>.</p> Scope Example Notes <code>tool</code> <code>tool:web_search</code> Must match the tool name used in <code>grok-agent.yaml</code>. <code>network</code> <code>network:api.tavily.com</code> Hostname match. Globs allowed: <code>*.arxiv.org</code>. <code>network</code> <code>network:*</code> Wildcard \u2014 triggers a scanner warning. <code>filesystem</code> <code>filesystem:/tmp/cache/*</code> Agents are sandboxed to <code>/tmp</code> by default. <code>env</code> <code>env:TAVILY_API_KEY</code> Narrows which env vars the tool sees."},{"location":"guides/safety-profiles/#scan-vs-validate","title":"Scan vs validate","text":"<ul> <li><code>grok-install validate</code> enforces the schema (hard errors).</li> <li><code>grok-install scan</code> is opinionated: it flags things that are   legal but risky (<code>network:*</code>, missing approvals on <code>post_*</code> tools,   unbounded <code>max_turns</code>, etc.). Ship-readiness check, not a build gate.</li> </ul>"},{"location":"guides/safety-profiles/#common-misconfigurations","title":"Common misconfigurations","text":"<p>Tool listed in <code>tools:</code>, missing from <code>permissions:</code></p> <p>Hard error at <code>validate</code>. The fix is one line in <code>grok-security.yaml</code>.</p> <p><code>network:*</code> with <code>safety_profile: strict</code></p> <p>Legal, but <code>grok-install scan</code> will flag it. Consider narrowing the host list \u2014 strict profiles usually shouldn't need the open web.</p> <p>Approval on a tool that doesn't exist</p> <p><code>requires_approval: [post_thread]</code> + no such tool \u2192 validation error. Cross-checked at validate time.</p>"},{"location":"guides/tool-schemas/","title":"Tool schemas","text":"<p>Tools are the verbs an agent can use. They're declared by name in <code>grok-agent.yaml</code>, scoped in <code>grok-security.yaml</code>, and backed by a function the runtime resolves at boot.</p>"},{"location":"guides/tool-schemas/#declaring-a-tool","title":"Declaring a tool","text":"<p>Inside your agent repo, drop a Python file under <code>tools/</code>:</p> <pre><code># tools/now.py\nfrom grok_install.tools import tool\nfrom datetime import datetime, timezone\n\n@tool(\n    name=\"now\",\n    description=\"Return the current UTC time as an ISO-8601 string.\",\n    parameters={},\n)\ndef now() -&gt; str:\n    return datetime.now(timezone.utc).isoformat(timespec=\"seconds\")\n</code></pre> <p>The decorator registers the tool with the runtime and generates the JSON Schema the model uses to decide when to call it.</p>"},{"location":"guides/tool-schemas/#parameters","title":"Parameters","text":"<pre><code>@tool(\n    name=\"web_search\",\n    description=\"Search the open web via Tavily.\",\n    parameters={\n        \"query\": {\"type\": \"string\", \"description\": \"Natural-language query\"},\n        \"max_results\": {\"type\": \"integer\", \"minimum\": 1, \"maximum\": 20, \"default\": 5},\n    },\n    required=[\"query\"],\n)\ndef web_search(query: str, max_results: int = 5) -&gt; list[dict]:\n    ...\n</code></pre> <p>The <code>parameters</code> block is a subset of JSON Schema. The runtime validates the model's arguments before the function ever runs \u2014 malformed calls are returned to the model as errors.</p>"},{"location":"guides/tool-schemas/#return-values","title":"Return values","text":"<p>Return anything JSON-serializable. Common patterns:</p> <ul> <li>Plain string \u2192 model treats as free-text.</li> <li><code>list[dict]</code> \u2192 rendered as a numbered table in the conversation log.</li> <li><code>dict</code> with an <code>error:</code> key \u2192 the runtime surfaces it as a tool   failure, and the agent can retry.</li> </ul>"},{"location":"guides/tool-schemas/#wiring-to-an-agent","title":"Wiring to an agent","text":"<pre><code># .grok/grok-agent.yaml\nagents:\n  - id: greeter\n    model: grok-4\n    prompt_ref: greeter_system\n    tools:\n      - now\n</code></pre> <p>Every name in <code>tools:</code> must:</p> <ol> <li>Resolve to a registered <code>@tool</code> in your <code>tools/</code> directory.</li> <li>Appear under <code>permissions:</code> in <code>grok-security.yaml</code>:    <pre><code>permissions:\n  - tool:now\n</code></pre></li> </ol> <p>If either check fails, <code>grok-install validate</code> errors before you can run.</p>"},{"location":"guides/tool-schemas/#requiring-approval","title":"Requiring approval","text":"<p>High-impact tools (outbound posts, payments, destructive file ops) should be human-gated:</p> <pre><code># .grok/grok-security.yaml\nsafety_profile: strict\npermissions:\n  - tool:post_reply\n  - network:api.twitter.com\nrequires_approval:\n  - post_reply\n</code></pre> <p>At runtime, every invocation of <code>post_reply</code> pauses the agent and prompts the operator with the proposed arguments. See Safety profiles \u2192.</p>"},{"location":"guides/tool-schemas/#built-in-tool-library","title":"Built-in tool library","text":"<p>The runtime ships a standard library you can enable without writing code:</p> Tool Purpose Permissions needed <code>now</code> UTC timestamp <code>tool:now</code> <code>http_get</code> Fetch a URL <code>tool:http_get</code> + <code>network:*</code> <code>web_search</code> Tavily search <code>tool:web_search</code> + <code>network:api.tavily.com</code> <code>fetch_mentions</code> Pull X mentions for authed user <code>tool:fetch_mentions</code> + <code>network:api.twitter.com</code> <code>post_reply</code> Post a reply on X <code>tool:post_reply</code> (gate it!) <code>fetch_arxiv</code> Query arXiv <code>tool:fetch_arxiv</code> + <code>network:*.arxiv.org</code> <p>Enable one with:</p> <pre><code>tools:\n  - web_search\n</code></pre> <pre><code>permissions:\n  - tool:web_search\n  - network:api.tavily.com\nenv:\n  - TAVILY_API_KEY\n</code></pre>"},{"location":"guides/tool-schemas/#testing-tools","title":"Testing tools","text":"<p><code>grok-install test</code> runs the agent with mocks for every tool \u2014 useful for CI. Override a mock:</p> <pre><code>grok-install test . --mock now=2026-04-17T14:32:00+00:00\n</code></pre>"},{"location":"guides/x-integration/","title":"X integration","text":"<p>The payoff of <code>grok-install</code> is the install-on-X flow. This guide covers how it works, what triggers it, and how to make your agent discoverable.</p>"},{"location":"guides/x-integration/#the-install-flow","title":"The install flow","text":"<pre><code>sequenceDiagram\n    participant U as User on X\n    participant X as X / Grok\n    participant R as Your repo\n    participant G as grok-install runtime\n    U-&gt;&gt;X: Sees post with github.com/you/agent link\n    X-&gt;&gt;R: Fetches grok-install.yaml\n    X-&gt;&gt;X: Runs pre-install scan (same as `grok-install scan`)\n    X-&gt;&gt;U: Shows \"Install with Grok\" button\n    U-&gt;&gt;X: Taps Install\n    X-&gt;&gt;U: Prompts for env vars from `env:` list\n    X-&gt;&gt;G: Boots the runtime in an isolated sandbox\n    G-&gt;&gt;U: Agent is live \u2014 posts welcome message</code></pre>"},{"location":"guides/x-integration/#what-makes-a-repo-installable","title":"What makes a repo installable","text":"<ol> <li><code>grok-install.yaml</code> at the root.</li> <li><code>spec:</code> declared with a supported version (<code>v2.12</code> current).</li> <li>Clean safety scan \u2014 no hardcoded secrets, no unlisted network access.</li> <li>Public repo (or a linked install token for private ones).</li> </ol>"},{"location":"guides/x-integration/#manifest-fields-that-light-up-the-ui","title":"Manifest fields that light up the UI","text":"Field Where it shows <code>name</code> Install card title <code>description</code> Install card body <code>category</code> Marketplace filter <code>tags</code> Marketplace search <code>featured: true</code> Homepage rotation (maintainer-set, not user-set) <code>on_install.welcome</code> First post the agent makes after install"},{"location":"guides/x-integration/#grok-commands","title":"<code>@grok</code> commands","text":"<p>Once the agent is running, users interact with it via <code>@grok</code>:</p> Command What it does <code>@grok install &lt;repo&gt;</code> Installs the agent from any GitHub repo. <code>@grok my agents</code> Opens the user's dashboard. <code>@grok pause &lt;agent&gt;</code> Pauses a running agent. <code>@grok update &lt;agent&gt;</code> Re-runs install from the latest commit. <code>@grok remove &lt;agent&gt;</code> Uninstalls. <p>Voice commands mirror the text ones on mobile.</p>"},{"location":"guides/x-integration/#pre-install-scan","title":"Pre-install scan","text":"<p>Every install triggers a scan identical to <code>grok-install scan</code> locally. It reports:</p> <ul> <li>Safety profile (<code>standard</code> / <code>strict</code>)</li> <li>Permission summary (tools, network hosts, filesystem, env)</li> <li>Dependency audit (licensed, no known CVEs)</li> <li>Secret scan (no hardcoded keys)</li> </ul> <p>A clean scan earns the Verified by Grok badge on the install card. Failures block the button.</p>"},{"location":"guides/x-integration/#marketplace-visibility","title":"Marketplace visibility","text":"<p>To show up in the curated marketplace:</p> <ol> <li>Submit to    <code>awesome-grok-agents</code>    via the template submission flow.</li> <li>Run <code>grok-install publish</code> to generate the metadata block.</li> <li>Open a PR with your template under the matching category.</li> </ol> <p>Curated agents go through a human review + automated validation. Once merged they're visible to any user browsing via <code>@grok gallery</code>.</p>"},{"location":"guides/x-integration/#dm-handlers-and-reply-bots","title":"DM handlers and reply bots","text":"<p><code>x_native_runtime</code> sub-types specialize the install experience:</p> <pre><code># grok-install.yaml (reply bot)\nx_native_runtime:\n  type: reply-bot\n  permissions: [\"tweet.read\", \"tweet.write\"]\n  grok_orchestrator: true\n</code></pre> <code>type</code> Behavior <code>reply-bot</code> Watches mentions, posts replies (usually approval-gated). <code>dm-handler</code> Watches DMs, can converse privately. <code>trend-monitor</code> Scheduled; posts when certain trends fire. <code>custom</code> You own the event loop; runtime just supervises. <p>Each type ships sensible rate-limit defaults and required approvals. <code>reply-bot</code> automatically gates <code>post_reply</code> unless you opt out.</p>"},{"location":"guides/x-integration/#growth-via-the-passive-growth-engine","title":"Growth via the Passive Growth Engine","text":"<p>If you declare:</p> <pre><code>promotion:\n  auto_welcome: true\n  auto_share: true\n  weekly_highlight: true\n  builder_credits: \"@YourHandle\"\n</code></pre> <p>The runtime will:</p> <ul> <li>Post a welcome card when a user installs.</li> <li>Add a \"built with grok-install\" shareable card per run.</li> <li>Include you in the Sunday weekly-highlights thread if your agent   ranks well on install count and user-rated usefulness.</li> </ul> <p>All opt-in. If you leave <code>promotion:</code> off, none of this fires.</p>"},{"location":"playground/","title":"Playground","text":"<p>Paste any <code>grok-*.yaml</code> file. The validator auto-detects the file type, runs it against the official JSON schema, and reports line-scoped errors \u2014 all in the browser. No network round-trip.</p> YAML \u2014 load sample \u2014 hello-grok (minimal install) reply-engagement-bot research-swarm grok-agent.yaml grok-security.yaml (strict) Copy install command Download YAML Validation Booting\u2026"},{"location":"playground/#how-it-works","title":"How it works","text":"<ol> <li>YAML is parsed with <code>js-yaml</code>.</li> <li>The validator infers which spec file you're working on from the    top-level keys: <code>spec:</code> \u2192 install, <code>agents:</code> \u2192 agent, <code>workflow:</code> \u2192    workflow, <code>prompts:</code> \u2192 prompts, <code>safety_profile:</code> \u2192 security.</li> <li>It loads the matching JSON schema from <code>assets/schemas/v2.12/</code> and    validates with <code>Ajv</code> (<code>draft/2020-12</code>).</li> <li>Errors are reported by JSON pointer with the offending keyword    parameters.</li> </ol> <p>Everything runs in the browser \u2014 your YAML never leaves the page.</p>"},{"location":"playground/#schemas","title":"Schemas","text":"<ul> <li><code>grok-install.schema.json</code></li> <li><code>grok-agent.schema.json</code></li> <li><code>grok-workflow.schema.json</code></li> <li><code>grok-security.schema.json</code></li> <li><code>grok-prompts.schema.json</code></li> </ul> <p>Schemas are synced nightly from <code>agentmindcloud/grok-yaml-standards</code> via the <code>sync-schemas.yml</code> workflow. Versioned copies live under <code>assets/schemas/v&lt;VERSION&gt;/</code>.</p>"},{"location":"spec/","title":"Spec overview","text":"<p>The <code>grok-install</code> standard is five YAML files working together. A repo only needs <code>grok-install.yaml</code> at the root; everything else lives under <code>.grok/</code>.</p> <pre><code>my-agent/\n\u251c\u2500\u2500 grok-install.yaml          # (required) root manifest\n\u2514\u2500\u2500 .grok/\n    \u251c\u2500\u2500 grok-agent.yaml        # agent definitions\n    \u251c\u2500\u2500 grok-workflow.yaml     # (optional) multi-agent orchestration\n    \u251c\u2500\u2500 grok-prompts.yaml      # named system prompts\n    \u2514\u2500\u2500 grok-security.yaml     # safety profile, permissions, limits\n</code></pre>"},{"location":"spec/#the-five-files","title":"The five files","text":""},{"location":"spec/#grok-installyaml","title":"grok-install.yaml \u2192","text":"<p>The one required file. Declares the spec version, name, entrypoint, runtime, and environment variables.</p>"},{"location":"spec/#grok-agentyaml","title":"grok-agent.yaml \u2192","text":"<p>Defines one or more agents: id, model, prompt reference, available tools, turn limits.</p>"},{"location":"spec/#grok-workflowyaml","title":"grok-workflow.yaml \u2192","text":"<p>For swarms. A sequence of typed steps that chain agents together with inputs, outputs, and conditions.</p>"},{"location":"spec/#grok-promptsyaml","title":"grok-prompts.yaml \u2192","text":"<p>System prompts lookup table. Agents reference prompts by key via <code>prompt_ref</code>.</p>"},{"location":"spec/#grok-securityyaml","title":"grok-security.yaml \u2192","text":"<p>Safety profile, explicit permissions, approval gates, and rate limits enforced by the runtime.</p>"},{"location":"spec/#version-matrix","title":"Version matrix","text":"Spec version Released Notable changes v2.12 Apr 2026 Passive Growth Engine, voice controls v2.10 Mar 2026 Verified-by-Grok badge, minimum-keys-only v2.4 Feb 2026 Orchestration + triggers v2.0 Jan 2026 Multi-LLM providers v1.0 Dec 2025 Initial standard <p>Pin your spec version</p> <p>Declare it explicitly at the top of <code>grok-install.yaml</code>: <pre><code>spec: grok-install/v2.12\n</code></pre> This locks the validator and runtime behavior, so upstream changes can't silently break your agent.</p>"},{"location":"spec/#json-schemas","title":"JSON schemas","text":"<p>Every file has a published JSON Schema for editor integration and the live playground:</p> <ul> <li><code>grok-install.schema.json</code></li> <li><code>grok-agent.schema.json</code></li> <li><code>grok-workflow.schema.json</code></li> <li><code>grok-security.schema.json</code></li> <li><code>grok-prompts.schema.json</code></li> </ul> <p>Versioned copies live at <code>assets/schemas/v&lt;VERSION&gt;/</code>. A CI job syncs them nightly from <code>agentmindcloud/grok-yaml-standards</code>.</p>"},{"location":"spec/grok-agent-yaml/","title":"<code>grok-agent.yaml</code>","text":"<p>Defines the agents that do the work. Lives at <code>.grok/grok-agent.yaml</code>.</p>"},{"location":"spec/grok-agent-yaml/#minimal-example","title":"Minimal example","text":"<pre><code>agents:\n  - id: greeter\n    model: grok-4\n    prompt_ref: greeter_system\n    tools:\n      - now\n    max_turns: 4\n</code></pre>"},{"location":"spec/grok-agent-yaml/#full-example-three-agent-pipeline","title":"Full example (three-agent pipeline)","text":"<pre><code>agents:\n  - id: triage\n    model: grok-4\n    prompt_ref: triage_system\n    tools:\n      - fetch_mentions\n      - classify_mention\n    max_turns: 6\n\n  - id: drafter\n    model: grok-4\n    prompt_ref: drafter_system\n    tools:\n      - draft_reply\n    max_turns: 4\n\n  - id: publisher\n    model: grok-4\n    prompt_ref: publisher_system\n    tools:\n      - post_reply\n    max_turns: 2\n</code></pre> <p>Agents don't execute in a defined order on their own \u2014 ordering is the job of <code>grok-workflow.yaml</code>.</p>"},{"location":"spec/grok-agent-yaml/#field-reference","title":"Field reference","text":"<p>Top-level key:</p> Field Type Required Notes <code>agents</code> list \u2713 Must contain at least one agent. <p>Each entry in <code>agents</code>:</p> Field Type Required Default Notes <code>id</code> string \u2713 \u2014 <code>snake_case</code>. Unique within the file. Referenced by workflow steps. <code>model</code> string \u2713 \u2014 e.g. <code>grok-4</code>, <code>grok-4-mini</code>. Overrides the root <code>model</code>. <code>prompt_ref</code> string \u2713 \u2014 Key in <code>grok-prompts.yaml</code> \u2192 <code>prompts:</code>. <code>tools</code> list <code>[]</code> Tool names. Must appear in <code>grok-security.yaml</code> \u2192 <code>permissions</code>. <code>max_turns</code> integer 6 Hard upper bound on agent\u2194tool round-trips. 1\u2013100. <code>temperature</code> number model default 0.0\u20132.0. Usually leave unset."},{"location":"spec/grok-agent-yaml/#patterns","title":"Patterns","text":""},{"location":"spec/grok-agent-yaml/#single-agent","title":"Single-agent","text":"<p>One agent, <code>entrypoint: .grok/grok-agent.yaml</code>, no workflow file needed. The runtime invokes the first <code>agents[]</code> entry with the user input.</p>"},{"location":"spec/grok-agent-yaml/#multi-step-sequential","title":"Multi-step (sequential)","text":"<p>Multiple agents, each with a narrow job. You need a <code>grok-workflow.yaml</code> to chain them in order.</p>"},{"location":"spec/grok-agent-yaml/#swarm","title":"Swarm","text":"<p>Multiple agents, dynamic routing (e.g. conditional loops for critique \u2192 refine). Same structure; the workflow file carries the control flow.</p>"},{"location":"spec/grok-agent-yaml/#validation-notes","title":"Validation notes","text":"<ul> <li><code>prompt_ref</code> is cross-validated against <code>grok-prompts.yaml</code> at   <code>grok-install validate</code> time.</li> <li><code>tools</code> are cross-validated against <code>grok-security.yaml</code>. Listing a   tool here that isn't permitted there is a hard error.</li> </ul> <p>Schema: <code>grok-agent.schema.json</code></p>"},{"location":"spec/grok-install-yaml/","title":"<code>grok-install.yaml</code>","text":"<p>The root manifest. Every installable agent has exactly one at the repo root.</p>"},{"location":"spec/grok-install-yaml/#minimal-example","title":"Minimal example","text":"<pre><code>spec: grok-install/v2.12\nname: hello-grok\ndescription: The simplest possible Grok agent. Single agent, single tool.\nentrypoint: .grok/grok-agent.yaml\nmodel: grok-4\nruntime:\n  python: \"&gt;=3.11\"\nenv:\n  - XAI_API_KEY\n</code></pre> <p>This is the real content of the <code>hello-grok</code> template. Three required fields, four optional \u2014 enough to boot.</p>"},{"location":"spec/grok-install-yaml/#full-example-scheduled-reply-bot","title":"Full example (scheduled reply bot)","text":"<pre><code>spec: grok-install/v2.12\nname: reply-engagement-bot\ndescription: Watches X mentions and drafts thoughtful replies behind an approval gate.\nentrypoint: .grok/grok-workflow.yaml\nmodel: grok-4\n\nruntime:\n  python: \"&gt;=3.11\"\n\nenv:\n  - XAI_API_KEY\n  - X_BEARER_TOKEN\n  - X_API_KEY\n  - X_API_SECRET\n  - X_ACCESS_TOKEN\n  - X_ACCESS_SECRET\n\nschedule:\n  interval: 5m\n\ncategory: social\ntags: [\"reply-bot\", \"x\", \"approval-gated\"]\nfeatured: true\n</code></pre>"},{"location":"spec/grok-install-yaml/#field-reference","title":"Field reference","text":"Field Type Required Default Notes <code>spec</code> string \u2713 \u2014 Must match <code>grok-install/v&lt;MAJOR&gt;.&lt;MINOR&gt;</code>. Pin it. <code>name</code> string \u2713 \u2014 Agent display name. kebab-case. 1\u201360 chars. <code>description</code> string \u2713 \u2014 10\u2013200 chars. Shown in the install card. <code>entrypoint</code> string (path) \u2014 <code>.grok/grok-agent.yaml</code> or <code>.grok/grok-workflow.yaml</code>. <code>model</code> string \u2014 Default LLM for all agents. Overridable per-agent. <code>runtime</code> object \u2014 Runtime constraints. See below. <code>env</code> list of string <code>[]</code> Env var names the agent reads. Runtime prompts for each on install. <code>schedule</code> object \u2014 Cron-like triggers for non-interactive agents. <code>category</code> string \u2014 <code>social</code>, <code>research</code>, <code>code</code>, <code>voice</code>, <code>utility</code>, etc. <code>tags</code> list of string <code>[]</code> Searchable keywords in the agent marketplace. <code>featured</code> boolean <code>false</code> Set by maintainers of curated galleries; not user-writable."},{"location":"spec/grok-install-yaml/#runtime","title":"<code>runtime</code>","text":"Key Type Notes <code>python</code> string PEP 440 specifier, e.g. <code>\"&gt;=3.11\"</code> <code>node</code> string semver range for Node-based agents"},{"location":"spec/grok-install-yaml/#schedule","title":"<code>schedule</code>","text":"Key Type Notes <code>interval</code> string Duration literal: <code>30s</code>, <code>5m</code>, <code>2h</code>, <code>1d</code>. <code>cron</code> string Standard 5-field cron expression. Mutually exclusive with <code>interval</code>."},{"location":"spec/grok-install-yaml/#common-mistakes","title":"Common mistakes","text":"<p>Quoting the spec version</p> <pre><code>spec: grok-install/v2.12     # \u2713\nspec: \"grok-install/v2.12\"   # \u2713 (YAML strings accept either)\nspec: grok-install/2.12      # \u2717 missing the `v` prefix\n</code></pre> <p>Env vars with lowercase</p> <p>Env var names must be <code>UPPER_SNAKE_CASE</code>. The validator rejects <code>xai_api_key</code>.</p> <p>Entrypoint outside <code>.grok/</code></p> <p>The entrypoint must live under <code>.grok/</code>. Putting it at the repo root pollutes the namespace that tools like <code>grok-agent</code> reserve.</p>"},{"location":"spec/grok-install-yaml/#validation","title":"Validation","text":"<p>Client-side: use the playground.</p> <p>CLI:</p> <pre><code>grok-install validate .\n</code></pre> <p>Schema: <code>grok-install.schema.json</code></p>"},{"location":"spec/grok-prompts-yaml/","title":"<code>grok-prompts.yaml</code>","text":"<p>Prompts live in one file, keyed by name. Agents reference them by <code>prompt_ref</code> in <code>grok-agent.yaml</code>. This separation lets you iterate prompts without touching agent wiring, and it keeps diffs clean when a prompt changes.</p>"},{"location":"spec/grok-prompts-yaml/#minimal-example","title":"Minimal example","text":"<pre><code>prompts:\n  greeter_system: |\n    You are a friendly Grok agent whose only job is to greet the user and,\n    if asked, tell them the current time via the `now` tool. Keep replies\n    to two sentences.\n</code></pre>"},{"location":"spec/grok-prompts-yaml/#full-example-three-prompts-for-a-research-swarm","title":"Full example (three prompts for a research swarm)","text":"<pre><code>prompts:\n  researcher_system: |\n    You gather sources for the question. Rules:\n    - Prefer primary sources (papers, docs, first-party blog posts) over\n      aggregators.\n    - Call `add_source` for every claim you want to carry forward, including\n      URL, author, date, and a one-sentence extract.\n    - Stop when you have 5-8 high-quality sources or you've done 10 searches.\n\n  critic_system: |\n    You are the skeptical peer reviewer. For each claim in the findings:\n    - Try to falsify it with `challenge_claim`.\n    - Score the evidence strength 0-1 with `score_evidence`.\n    - Flag any claim that rests on a single source or a non-primary source\n      as a weak_claim the researcher must revisit.\n\n  publisher_system: |\n    Compile a brief with: TL;DR (2 sentences), 3-5 key findings, open\n    questions, sources cited inline. Never introduce claims the critic\n    rejected. Never add sources that aren't in the findings list.\n</code></pre>"},{"location":"spec/grok-prompts-yaml/#field-reference","title":"Field reference","text":"Field Type Required Notes <code>prompts</code> object \u2713 Map of prompt name \u2192 prompt body. At least one entry. <p>Each key:</p> Constraint Rule Name <code>snake_case</code>, starts with a letter. Body String. Usually written as a YAML block scalar (<code>|</code>) for multi-line."},{"location":"spec/grok-prompts-yaml/#style-guidance","title":"Style guidance","text":"<ul> <li>Name prompts after the role they fill, not the model. <code>researcher_system</code> \u2014 not <code>grok4_research_v2</code>.</li> <li>Keep each prompt under ~300 words. Longer prompts fight the model.</li> <li>Put tool-use rules in the prompt, even though tools are declared   separately \u2014 the model needs to know when to reach for each.</li> <li>Use second-person imperative (\"You gather sources\"), not third (\"The   agent should gather sources\").</li> </ul>"},{"location":"spec/grok-prompts-yaml/#cross-validation","title":"Cross-validation","text":"<p><code>grok-install validate</code> checks that every <code>prompt_ref</code> used in <code>grok-agent.yaml</code> resolves to a key in <code>prompts:</code>. Orphaned prompts (defined here but never referenced) produce a warning, not an error \u2014 useful while iterating.</p> <p>Schema: <code>grok-prompts.schema.json</code></p>"},{"location":"spec/grok-security-yaml/","title":"<code>grok-security.yaml</code>","text":"<p>Declarative security. If a tool or network host isn't listed here, the runtime refuses to invoke it \u2014 even if the agent asks.</p>"},{"location":"spec/grok-security-yaml/#minimal-standard-profile","title":"Minimal (standard profile)","text":"<pre><code>safety_profile: standard\npermissions:\n  - tool:now\nrequires_approval: []\nrate_limits:\n  tool_calls_per_minute: 30\n</code></pre>"},{"location":"spec/grok-security-yaml/#full-example-research-swarm","title":"Full example (research swarm)","text":"<pre><code>safety_profile: standard\npermissions:\n  - tool:web_search\n  - tool:fetch_page\n  - tool:add_source\n  - tool:challenge_claim\n  - tool:score_evidence\n  - tool:compile_brief\n  - network:api.tavily.com\n  - network:*.arxiv.org\n  - network:*          # research must browse the open web\nrequires_approval: []\nrate_limits:\n  tool_calls_per_minute: 40\n  web_fetches_per_run: 60\n</code></pre>"},{"location":"spec/grok-security-yaml/#full-example-strict-approval-gated","title":"Full example (strict, approval-gated)","text":"<pre><code>safety_profile: strict\npermissions:\n  - tool:fetch_mentions\n  - tool:classify_mention\n  - tool:draft_reply\n  - tool:post_reply\n  - network:api.twitter.com\nrequires_approval:\n  - post_reply        # human must confirm every outbound post\nrate_limits:\n  tool_calls_per_minute: 15\n  web_fetches_per_run: 20\n</code></pre>"},{"location":"spec/grok-security-yaml/#field-reference","title":"Field reference","text":"Field Type Required Notes <code>safety_profile</code> enum \u2713 <code>standard</code> or <code>strict</code>. <code>permissions</code> list of string \u2713 Scoped grants. Format: <code>&lt;scope&gt;:&lt;target&gt;</code>. Supported scopes below. <code>requires_approval</code> list of string Tool names. Each invocation pauses for human confirmation before running. <code>rate_limits</code> object Soft caps enforced by the runtime. Agent sees a rate-limit error when exceeded."},{"location":"spec/grok-security-yaml/#permission-scopes","title":"Permission scopes","text":"Scope Target form Example <code>tool</code> tool name (snake_case) <code>tool:web_search</code> <code>network</code> hostname or glob pattern <code>network:api.tavily.com</code>, <code>network:*.arxiv.org</code>, <code>network:*</code> <code>filesystem</code> absolute path or glob <code>filesystem:/tmp/cache/*</code> <code>env</code> env var name <code>env:TAVILY_API_KEY</code>"},{"location":"spec/grok-security-yaml/#rate_limits-keys","title":"<code>rate_limits</code> keys","text":"Key Type Typical range Notes <code>tool_calls_per_minute</code> integer 10\u201360 Hard ceiling across all tools. <code>web_fetches_per_run</code> integer 10\u2013200 Only meaningful if <code>network:*</code> is granted. <code>total_tokens_per_run</code> integer 10k\u2013500k Abort the run if LLM token budget exceeds."},{"location":"spec/grok-security-yaml/#safety-profiles","title":"Safety profiles","text":"Profile What changes <code>standard</code> Read-heavy research. Side-effect tools get logged but aren't blocked by default. <code>strict</code> Every destructive or outbound action requires an explicit entry in <code>requires_approval</code>. Rate limits are enforced more tightly. <p>Read the deeper guide: Safety profiles \u2192</p>"},{"location":"spec/grok-security-yaml/#validation-notes","title":"Validation notes","text":"<ul> <li>A tool listed in <code>grok-agent.yaml</code> that isn't in <code>permissions</code> is a   hard error at <code>grok-install validate</code> time.</li> <li><code>network:*</code> is allowed but lights up a warning in <code>grok-install scan</code>.</li> <li><code>requires_approval</code> entries must also appear in <code>permissions</code> \u2014 you   can't gate something the agent isn't allowed to touch.</li> </ul> <p>Schema: <code>grok-security.schema.json</code></p>"},{"location":"spec/grok-workflow-yaml/","title":"<code>grok-workflow.yaml</code>","text":"<p>Orchestration for multi-step and swarm agents. Lives at <code>.grok/grok-workflow.yaml</code>. Set <code>entrypoint: .grok/grok-workflow.yaml</code> in your root manifest to activate it.</p>"},{"location":"spec/grok-workflow-yaml/#example-researcher-critic-publisher-swarm","title":"Example: researcher \u2192 critic \u2192 publisher swarm","text":"<pre><code>workflow:\n  id: research_swarm\n  input_schema:\n    question: string\n  steps:\n    - id: research_round_one\n      agent: researcher\n      input:\n        question: \"{{ input.question }}\"\n      output: initial_findings\n\n    - id: critique\n      agent: critic\n      input:\n        findings: \"{{ initial_findings }}\"\n      output: critiques\n\n    - id: research_round_two\n      agent: researcher\n      when: \"{{ critiques.weak_claims | length &gt; 0 }}\"\n      input:\n        question: \"{{ input.question }}\"\n        gaps: \"{{ critiques.weak_claims }}\"\n      output: refined_findings\n\n    - id: final_brief\n      agent: publisher\n      input:\n        question: \"{{ input.question }}\"\n        findings: \"{{ refined_findings | default(initial_findings) }}\"\n        critiques: \"{{ critiques }}\"\n      output: brief\n</code></pre> <p>Notice the conditional <code>when:</code> on <code>research_round_two</code> \u2014 the critic's output determines whether the researcher runs a second pass.</p>"},{"location":"spec/grok-workflow-yaml/#field-reference","title":"Field reference","text":"<p>Top-level:</p> Field Type Required <code>workflow</code> object \u2713 <p><code>workflow</code>:</p> Field Type Required Notes <code>id</code> string \u2713 <code>snake_case</code> identifier for the flow. <code>input_schema</code> object Map of input name \u2192 type (<code>string</code>, <code>number</code>, <code>boolean</code>, <code>object</code>, <code>array</code>). <code>steps</code> list \u2713 Ordered list of steps; at least one. <p>Each step:</p> Field Type Required Notes <code>id</code> string \u2713 <code>snake_case</code>. Output values are addressable by this id. <code>agent</code> string \u2713 Must match an <code>id</code> in <code>grok-agent.yaml</code>. <code>input</code> object Arbitrary key \u2192 Jinja expression. <code>{{ input.x }}</code> references flow inputs; <code>{{ step_id }}</code> references prior output. <code>output</code> string Binds this step's result to a variable name for downstream steps. <code>when</code> string Jinja boolean expression. Step is skipped if falsy."},{"location":"spec/grok-workflow-yaml/#templating","title":"Templating","text":"<p>Step inputs are Jinja2 strings evaluated at runtime. You get:</p> <ul> <li><code>{{ input.&lt;key&gt; }}</code> \u2014 original flow input.</li> <li><code>{{ &lt;step_output_name&gt; }}</code> \u2014 any prior step's <code>output</code>.</li> <li>Standard filters: <code>default</code>, <code>length</code>, <code>map</code>, <code>select</code>, <code>join</code>, <code>tojson</code>.</li> </ul> <pre><code>input:\n  findings: \"{{ refined_findings | default(initial_findings) }}\"\n</code></pre>"},{"location":"spec/grok-workflow-yaml/#error-behavior","title":"Error behavior","text":"<ul> <li>A step that raises halts the workflow (no silent continue).</li> <li>A step with <code>when:</code> that evaluates falsy is logged as <code>skipped</code> and   the workflow continues.</li> <li>Output bindings survive skips: downstream <code>{{ some_output }}</code> is   <code>undefined</code> if the producing step was skipped \u2014 use <code>default()</code>.</li> </ul>"},{"location":"spec/grok-workflow-yaml/#validation-notes","title":"Validation notes","text":"<ul> <li>Every step's <code>agent:</code> must resolve against <code>grok-agent.yaml</code>.</li> <li>Output names must be unique within a workflow.</li> <li><code>when:</code> and <code>input:</code> expressions are parsed as Jinja at validate time   \u2014 syntax errors fail the build, not the run.</li> </ul> <p>Schema: <code>grok-workflow.schema.json</code></p>"}]}